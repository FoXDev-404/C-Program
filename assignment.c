#include<stdio.h>
#include<math.h>
// Assignment Operators (=, +=, -=, *=, /=, %=) or we can say shorthant operators.
int main() {
    float a = 100;
    float b = 7;
    a *= b; // a = a * b same as we can write [+=, -=, /=, %=, =, *=].
    printf("%f \n", a);
    return 0;
}